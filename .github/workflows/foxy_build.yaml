name: ROS 2 Foxy

on:
  push:
    branches: 
      - "feature/ros2"

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ros:foxy
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build
        run: . /opt/ros/foxy/setup.sh && ./setup.sh