name: ROS 2 Iron

on:
  push:
    branches:
     - feature/ros2

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ros:iron
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build
        run: . /opt/ros/iron/setup.sh && ./setup.sh